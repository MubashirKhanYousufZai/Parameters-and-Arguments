// Parameters and Arguments
function greek(num: number, num2: number): number {
    return num + num2;
}
let result: number = greek(24,6)
console.log(result);

function totalMarks(paper: number , paper2: number): number {
    return paper + paper2;
}
let exam: number = totalMarks(24 , 50)
console.log(exam);